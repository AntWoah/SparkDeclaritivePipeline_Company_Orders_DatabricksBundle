# The main pipeline for SDP_CompanyOrders_Bundle

resources:
  pipelines:
    SDP_CompanyOrders_Bundle_etl:
      name: SDP_CompanyOrders_Bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/SDP_CompanyOrders_Bundle_etl"

      libraries:
        - glob:
            include: ../src/SDP_CompanyOrders_Bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
